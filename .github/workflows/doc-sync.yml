name: Doc Sync
on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    name: Verify doc sync (placeholder — no user-visible surface yet)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check README exists
        run: test -f README.md
