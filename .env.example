# nFamily (Expo / React Native) — Environment Configuration
# Copy to .env (gitignored) and fill in per-environment values.
# EXPO_PUBLIC_* vars are inlined into the client bundle — never put secrets here.

# Runtime environment: development | staging | production
APP_ENV=development

# App version surfaced in the UI / diagnostics (usually set from package.json at build)
EXPO_PUBLIC_APP_VERSION=0.1.0

# OpenTelemetry collector endpoint for client traces (leave empty to disable)
EXPO_PUBLIC_OTEL_ENDPOINT=

# Sentry DSN for client error reporting (leave empty to disable)
EXPO_PUBLIC_SENTRY_DSN=
